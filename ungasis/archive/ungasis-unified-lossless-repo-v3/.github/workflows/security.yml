name: Security Notes

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  remind:
    runs-on: ubuntu-latest
    steps:
      - name: Security reminder
        run: |
          echo "Run secret scanning and dependency scanning before production."
          echo "Use least privilege. Do not store real secrets in the repository."
