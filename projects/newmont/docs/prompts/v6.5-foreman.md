# FOREMAN: Newmont v6.5 Data Integrity Fix
# Tool: Claude Code (Sonnet) | Token budget: <20K | Priority: QIM June 18

Working directory: D:\.projects\ungasis\projects\newmont
Stack: React 19, TS 5.8, Vite 8, Zustand, Recharts, Tailwind 4
Build: standalone dist/index.html (file:// protocol)
Data source: src/lib/real-data.ts (auto-generated, DO NOT edit)

## CONTEXT: real-data.ts contains these verified numbers
- totalRequisitions: 19292
- fillRate: 73.6, cancelRate: 21.1
- timeToFill.average: 78, timeToFill.median: 62.9
- statusDistribution: filled 14205, cancelled 4072, open 729, onHold 219, pendingApproval 67
- openReqsAging: under30/between30and60/between60and90/over90
- byFunction: array sorted desc (Mine Operations 4530, Mine Maintenance 2132, ...)
- byCountry: array sorted desc (AU 4836, CA 3836, GH 2889, ...)
- postings.total: 316
- holds.totalRecords: 23709, holds.uniqueReqIds: (count)

## WAVE 1 — Parallel (independent files, no overlap)

### TASK A: Fix ExecutiveDashboard.tsx
File: find the main dashboard component (likely src/components/ExecutiveDashboard.tsx or similar)
1. Ensure import { realData } from '../lib/real-data' exists
2. Replace ALL KPI values with realData references:
   - Avg TTF must show realData.timeToFill.average (78), NOT 80.1
   - Open Reqs must show realData.statusDistribution.open (729), NOT 733
   - On Hold must show realData.statusDistribution.onHold (219), NOT 212
   - Add Pending Approval card: realData.statusDistribution.pendingApproval (67)
3. Remove or wrap ALL trend badges in a conditional:
   - "+8.4% vs prior 12 mo" — no historical data exists, REMOVE
   - "+2.1 pts rolling 90-day" — no historical data exists, REMOVE  
   - "-6.3 days improving" — no historical data exists, REMOVE
   - "+0.4 pts watch" — REMOVE
   - Replace with static subtitle text like "all-time" or "current period"
4. Remove sparkline mini-charts OR make them invisible (no trend data to show)
5. Fix tooltip: investigate Recharts tooltip rendering on this page. Common fix: ensure CustomTooltip component returns null when not active, and that the tooltip container has proper z-index and position styling.

### TASK B: Fix Hold Analysis page
File: find the Hold Analysis component (search for "Hold Analysis" or "Reason for Hold" in src/)
1. Remove "Reactivation Rate" KPI card entirely (61% is invented)
2. Remove "Avg Hold Duration" KPI card (52 days is invented — no duration data in CSV)
3. Remove "Reason for Hold" chart entirely (Budget freeze, Hiring manager, Org restructure, Role scope, Visa — ALL invented)
4. Replace removed sections with centered text: "Hold reason and duration data not available in current CORE export. Contact Manuel Kassis for field access."
5. Keep "Total On Hold" card but wire to realData.statusDistribution.onHold (219)
6. Keep "Hold Aging" chart ONLY if it reads from realData.openReqsAging, otherwise replace with banner
7. "Holds by Country" — wire to real data if available, otherwise add sample banner
8. "Aged 90+ Days" card — wire to realData.openReqsAging.over90 if referring to open reqs, otherwise mark as sample
9. Add amber banner at TOP of page: "Partial data — hold duration and reasons require additional CORE fields"
   Style: background #1e293b, border 1px solid #f59e0b, color #f59e0b, padding 8px 16px, border-radius 8px, text-align center, margin-bottom 16px

### TASK C: Fix sidebar counts
File: find the navigation/sidebar component (search for "733" or "212" or "Requisitions" in nav)
1. Sidebar shows "Requisitions 733" — must be 729 (realData.statusDistribution.open)
2. Sidebar shows "Hold Analysis 212" — must be 219 (realData.statusDistribution.onHold)
3. Import realData and wire these counts dynamically

### TASK D: Create scripts/refresh.js
File: scripts/refresh.js (NEW FILE)
Create a Node.js script that chains: aggregate-csv.js then npm run build.
- const { execSync } = require('child_process')
- const path = require('path')
- Step 1: run node scripts/aggregate-csv.js (inherit stdio)
- Step 2: run npm run build (inherit stdio)  
- Step 3: print dist/index.html file size
- Step 4: print success message with timestamp
- Zero dependencies. Exit 0 success, 1 error. Add try/catch.

## WAVE 2 — Sequential (depends on Wave 1)

### TASK E: Add sample banners to mock pages
After Wave 1 completes:
1. Requisitions table page — add amber banner at top: "Sample data — requisition table will be populated from CORE export via Ingestion Zone"
   Same style as Task B banner
2. CandidatePipeline.tsx — verify amber banner exists at TOP of component (not just footer text). If missing, add one: "Sample data — pending CORE candidate and workflow exports"
   Same amber style

### TASK F: Build + Verify + Commit
1. Run: npm run build
2. Verify dist/index.html exists and print size (expect ~780-800 KB)
3. Run: git add -A
4. Run: git status (show what changed)
5. Run: git commit -m "fix(v6.5): wire all KPIs to real-data.ts, remove invented metrics, add sample banners"
6. Do NOT git push (I will push manually)

## CONSTRAINTS
- All colors as inline hex. NEVER use Tailwind color classes.
- Surgical edits only. Do NOT rewrite entire files.
- Do NOT modify real-data.ts or aggregate-csv.js
- Do NOT install new packages
- Read only files you need to edit — minimize token usage
- If a file cannot be found, skip that task and report what was skipped
