# v7.0 Wave 2 — Claude Prompt (Integration + Layout Polish)
> Paste this into a NEW Claude Code session after Wave 1 is merged.
> Paste after `/effort high` and `/goal`

---

Read CLAUDE.md.

TASK: v7.0 Wave 2 — Integrate new components + layout polish.
Do these 5 tasks IN ORDER. Run `npm run build` after each task.

---

### TASK 5: Replace old CandidatePipeline with blocker version

**Files:**
- DELETE: `src/components/modules/CandidatePipeline.tsx` (old mock data version)
- RENAME: `src/components/modules/CandidatePipelineBlocker.tsx` → `src/components/modules/CandidatePipeline.tsx`

**What to do:**
1. Read `CandidatePipelineBlocker.tsx` — verify it exports a default component
2. Delete the old `CandidatePipeline.tsx` (the one with FUNNEL_DATA mock array)
3. Rename `CandidatePipelineBlocker.tsx` to `CandidatePipeline.tsx`
4. Check `src/components/Views.tsx` — if it imports `CandidatePipeline`, the import path should still work since we kept the same filename
5. Check `src/App.tsx` — same check for imports

**Verify:** `npm run build`

---

### TASK 6: Wire DataDictionary into the app

**Files:**
- `src/App.tsx` (add route)
- `src/components/layout/Sidebar.tsx` (add nav item)

**What to do:**
1. In `App.tsx`:
   - Add import: `import DataDictionary from '@/components/modules/DataDictionary';`
   - In `renderModule()` switch, add: `case 'dictionary': return <DataDictionary />;`
2. In `Sidebar.tsx`:
   - Find the NAV array (list of sidebar navigation items)
   - Add a new entry: `{ id: 'dictionary', label: 'Data Dictionary', icon: ... }`
   - Place it LAST in the list (after all other items)
   - For the icon: use an inline SVG table/grid icon, or reuse an existing icon

**Verify:** `npm run build`

---

### TASK 7: Move Ingestion Zone to its own sidebar page

**Files:**
- `src/App.tsx` (remove upload JSX, add route)
- NEW: `src/components/modules/DataUpload.tsx`
- `src/components/layout/Sidebar.tsx` (add nav item)

**What to do:**

1. **Create `src/components/modules/DataUpload.tsx`:**
   - Cut the entire Ingestion Zone block from App.tsx (the `<div>` with "📥 Ingestion Zone")
   - Cut also: `handleFileUpload` function, `loadingFile` state, `errorMsg` state, `toast` state, `fileInputs` array, and the toast JSX at the bottom
   - Paste all of it into this new component
   - This component needs these imports:
     - `import { useDashboardStore } from '@/stores/dashboard';`
     - `import { loadCSVFile, getRequisitions, getHoldEvents, getPostings } from '@/lib/data-engine';`
   - Export as: `export default function DataUpload()`

2. **Clean up `src/App.tsx`:**
   - REMOVE all the upload-related code (already moved to DataUpload)
   - KEEP: the `useEffect` with `initLocalData` (IndexedDB loader) — this MUST stay
   - KEEP: all module imports and `renderModule()` switch
   - ADD import: `import DataUpload from '@/components/modules/DataUpload';`
   - ADD case: `case 'upload': return <DataUpload />;`
   - The toast JSX at the bottom of App.tsx can be removed (it's now in DataUpload)

3. **Update `Sidebar.tsx`:**
   - Add nav item: `{ id: 'upload', label: 'Data Upload', icon: ... }`
   - Place it BEFORE Data Dictionary (so order is: ... Holds, Data Upload, Data Dictionary)
   - Use an upload/arrow-up icon

**Verify:** `npm run build`

---

### TASK 8: Add sidebar collapse toggle

**Files:**
- `src/components/layout/Sidebar.tsx` (main change)
- `src/components/layout/AppShell.tsx` (adjust main content)

**What to do:**

1. **In Sidebar.tsx:**
   - Add a collapse button at the TOP of the sidebar
   - Icon: `«` when expanded, `»` when collapsed
   - State with localStorage:
     ```tsx
     const [collapsed, setCollapsed] = useState(() => {
       try { return localStorage.getItem('sidebar-collapsed') === 'true'; }
       catch { return false; }
     });
     const toggle = () => {
       const next = !collapsed;
       setCollapsed(next);
       localStorage.setItem('sidebar-collapsed', String(next));
     };
     ```
   - When collapsed: sidebar width = 48px, hide text labels, show only icons centered
   - When expanded: full sidebar as current
   - CSS: `transition: width 0.2s ease-out` on the sidebar container
   - Text labels: `opacity: 0` and `width: 0` when collapsed, with transition

2. **In AppShell.tsx:**
   - The sidebar passes or exposes `collapsed` state
   - Main content area adjusts its left margin/padding when sidebar collapses
   - `transition: margin-left 0.2s ease-out` on the main content

**Verify:** `npm run build`

---

### TASK 9: Remove candidates count badge from Sidebar

**File:** `src/components/layout/Sidebar.tsx`

**What to do:**
1. Find the nav item for candidates/pipeline
2. Remove the count badge/number next to it (was showing fake data)
3. Keep the nav item itself visible — just no number

**Verify:** `npm run build`

---

## FINAL STEP

After all 5 tasks pass:

```bash
npm run build
git add -A
git commit -m "feat(v7.0): wave 2 — integration, data upload page, sidebar collapse, data dictionary wired"
```

Report: final dist/index.html file size.
