# Drop-In Guide

Copy `public/data/` into your app. Use `rag/all_chunks_integrated.jsonl` for embeddings. Use `graph/nodes_all.jsonl` and `graph/edges_all.jsonl` for the Relationship Explorer. Use `lineage/source_refresh_queue_all.json` to refresh exact numeric values from approved sources.
