# Example environment file.
# Copy this to .env only if your app needs it.
# Never put real secrets in this example file.

APP_ENV=local
APP_NAME=ungasis-local-test
LOG_LEVEL=info

# Put real keys only in your private .env file, never in public files.
EXAMPLE_API_KEY=replace_with_private_key_in_private_env_file_only
