# Claude Hook Examples

These scripts are examples. Review them before enabling any hook in `.claude/settings.json`.

Recommended starting point:

- `block-dangerous-bash.sh` for obvious destructive shell commands.
- `pre-commit-lint.sh` for local lint/test habits.

Do not enable hooks that modify files, send messages, deploy, or change external systems without explicit review.
