# Copy to .env for local development only. Never commit real secrets.
PROJECT_NAME={{PROJECT_NAME}}
APP_ENV=local
LOG_LEVEL=info

# Example only; use server-side secret handling.
# AI_PROVIDER_API_KEY=
