# Context Folder

This folder is the compact, mutable memory layer for AI sessions.

Use it for:

- background the assistant needs often
- user preferences
- current stack
- glossary
- active decisions

Keep it shorter than formal docs. If a file becomes long, move durable detail into `docs/` and summarize here.
